syntax = "proto3";

package buildbarn.configuration.server;

// Used for both GRPC and HTTP listeners.
message TLSConfiguration {
  // File containing the TLS private key, unencrypted.
  string key_file = 1;
  // File containing the TLS certificate.
  string cert_file = 2;
}
